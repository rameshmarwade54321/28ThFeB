
	//Syntax
	
	//package package_name;
	
	//public class class_name  { class body }       //class declaration
	
	//public static void main (String [] args )   {  main method body  }
	
	//System.out.println("printing message");    //printing statement
	
	//String variable_name  ="variable_value";    //variable declaration and variable initialization
	
	//int variable_name = number;
	
	//float variable_name = number_with_decimalf;    //50.5f   //there is no space between number & f
	
	//char variable_name = 'Single_charachter';
	
	//public static void method_name( ) { static regular method body }
	//public static void method_name( without parameters ) { static regular method body }
	//method_name( );   //called static method without parameter from same class main method
	//otherclass_name.otherclass_methodname(); //called static method without parameter from other class
	
	//public static void method_name( with parameters ) { static regular method body }
	//method_name(variable initialization ); //called static method with parameter from same class main method
	//otherclass_name.otherclass_method_name(variable initialization );  //called static method with parameter from other class
	
	//public  void method_name( ) { non static regular method body  }
	//public  void method_name( without parameter) { non static regular  method body  }
	//sameclass_name object_name = new sameclass_name();    //create object //same class
	//object_name.sameclass_method_name();   //called non static method in same class after creating a object
	//otherclass_name object_name = new otherclass_name(); //create object for other class for non static method
	//object_name.othetclass_method_name(); //called non static method without parameter from other class
	
	//public  void method_name( with parameter) { non static regular  method body  }
	//sameclass_name object_name = new sameclass_name();    //create object for same class
	//object_name.sameclass_method_name(variable initialization); //call method in same class
	//otherclass_name object_name = new otherclass_name();    //create object for other class
	//object_name.otherclass_method_name(variable initialization); //call method from other class
	
	
	//if (condition) {if body}       //if condition : >= , <= , == , > , < 
	
	//if (condition) {if body}
	//else  {else body}
	
	//if (condition) {if   body}
	//else if (condition)  {else   if   body}
	
	
